name: Release to Github

on:
  push:
    branches:
    - dev

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - name: Version
      id: version
      uses: PaulHatch/semantic-version@v5.3.0
      with:
        tag_prefix: ""
    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v1
    - name: Build
      run: dotnet build -c -p:Version=${VERSION} Release
      env:
        VERSION: ${{ steps.version.outputs.version }}.${{ steps.version.outputs.increment }}
    - name: Test
      run: dotnet test -c Release -p:Version=${VERSION} --no-build
    - name: Pack nugets
      run: dotnet pack -p:PackageVersion=${PACKAGE_VERSION} -c Release --no-build --output .
      env:
        DOTNET_CLI_TELEMETRY_OPTOUT: 1
        PACKAGE_VERSION: ${{ steps.version.outputs.version }}

    - name: Git Semantic Version
      uses: PaulHatch/semantic-version@v5.3.0
    - name: Push to NuGet
      run: dotnet nuget push "*.nupkg" --api-key ${{ secrets.GITHUB_TOKEN }} --source https://nuget.pkg.github.com/blastmanager/index.json
